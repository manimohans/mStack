interface:
  display_name: "Product Context"
  short_description: "Organize messy launch inputs before drafting."
  default_prompt: "Turn these product notes into a launch context brief. Separate confirmed facts, assumptions, missing proof, and claims to avoid."
