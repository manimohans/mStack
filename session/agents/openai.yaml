interface:
  display_name: "Session"
  short_description: "Track a launch workspace across mStack steps."
  default_prompt: "Create, inspect, or audit an mStack launch session. Show the artifact manifest, readiness verdict, stage status, and next specialist command to run."
